# Runtime state: per-machine evolution proposals, evolve reports, and patches.
# These are generated by `autobuilder postmortem` / `autobuilder evolve` and
# should never be version-controlled — they're observations about local runs,
# not assets that travel with the skill.
proposals/
