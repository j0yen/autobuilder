{
  "schema": "autobuilder.intent_card.v1",
  "prd_source": "/home/jsy/Documents/PRDs/build-queue/PRD-autobuilder-reviewer-intent-card-sha.md",
  "intent_slug": "autobuilder-reviewer-intent-card-sha",
  "root_motivation": "**Who:** the build loop's gate on RedBaron (extend-gate's reviewer phase \u2192 `autobuilder reviewer-agent finalize`) and the operator reading its GATES line.",
  "user_persona": "The autobuilder skill orchestrator (a Claude session) and the human invoking `/autobuilder` from their own project. Both shell out to this binary for the receipt + gate steps that must not live in bash.",
  "unfakeable_metric": {
    "name": "stage4_receipt_producers_callable",
    "lower_is_better": false,
    "harness_command": "scripts/run-metrics.sh",
    "target": 5
  },
  "acceptance_criteria": [
    {
      "id": "AC1",
      "level": "MUST",
      "description": "Given a reviewer object with `intent_card_sha` = bare 64-hex of the current card, When `finalize` runs, Then it succeeds and the written receipt has `intent_card_sha` = `sha256:<hex>`.",
      "test": "tests/acceptance_ac1.rs"
    },
    {
      "id": "AC2",
      "level": "MUST",
      "description": "Given `intent_card_sha` = `sha256:<hex>` (today's form) or the same hex in uppercase, When `finalize` runs, Then it succeeds with the canonical prefixed lowercase form written.",
      "test": "tests/acceptance_ac2.rs"
    },
    {
      "id": "AC3",
      "level": "MUST",
      "description": "Given `intent_card_sha` = the hash of a different card, When `finalize` runs, Then it fails and the error contains the given value, the normalized given hex, and the current card's hex.",
      "test": "tests/acceptance_ac3.rs"
    },
    {
      "id": "AC4",
      "level": "MUST",
      "description": "Given `intent_card_sha` of 63 hex chars or containing non-hex, When `finalize` runs, Then it fails with a message naming `intent_card_sha` and the expected `sha256:<64 hex>` form.",
      "test": "tests/acceptance_ac4.rs"
    },
    {
      "id": "AC5",
      "level": "MUST",
      "description": "Given the 2026-09-17 15:54:42Z `review-output.json` object and its intent card stored as a fixture, When `finalize` runs against a fixture project at that head, Then the receipt is written with `decision=block` and `block_reasons=[\"must-ac5-extend-gate-verification-missing\"]`.",
      "test": "tests/acceptance_ac5.rs"
    },
    {
      "id": "AC6",
      "level": "MUST",
      "description": "Given the two prompt sites, When grepped, Then both contain the sentence that `intent_card_sha` is copied verbatim from `review-request.json`'s `intent_card_sha256`, and the two commits (rustbuild prompt repo, build-skill) are cited in this PRD's receipts.",
      "test": "tests/acceptance_ac6.rs"
    },
    {
      "id": "AC7",
      "level": "MUST",
      "description": "Given reviewer.rs, When its unit test runs, Then the doc-comment table above `finalize` lists exactly the fields compared by string equality against request-provided values, and each has a normalization entry.",
      "test": "tests/acceptance_ac7.rs"
    },
    {
      "id": "AC8",
      "level": "MUST",
      "description": "Given `cargo test -p autobuilder rvsha_` on RedBaron, When it runs, Then all `rvsha_*` tests pass and the workspace clippy/deny/test gates are unchanged from baseline.",
      "test": "tests/acceptance_ac8.rs"
    },
    {
      "id": "AC9",
      "level": "SHOULD",
      "description": "Given `prepare` runs, When `review-request.json` is written, Then it carries `intent_card_sha` and `intent_card_sha256` with identical values.",
      "test": "tests/acceptance_ac9.rs"
    },
    {
      "id": "AC10",
      "level": "MUST",
      "description": "Given this PRD has landed and the producer was reinstalled on RedBaron, When `autobuilder --version` is read and the 2026-09-17 15:54:42Z fixture object is finalized with the installed binary inside a real worktree at its head, Then the version is above 0.9.0 and the journal shows `reviewer-agent finalize: decision=block` for that run with no `finalize rejected` note. (Live; evidence: `cmd:autobuilder --version` and `journal:reviewer-agent finalize: decision=block`; never deferrable \u2014 Joe 2...",
      "test": "tests/acceptance_ac10.rs"
    }
  ],
  "scope": [
    "Stage 1 intake (intent-card validation)",
    "Stage 2 scaffold (templated project materialization)",
    "Stage 3 iterate-and-prove loop (advance/revert)",
    "Stage 4 risk gate (7 receipts + release receipt)",
    "Stage 5 postmortem + gated self-evolution",
    "Companion shell scripts in ~/.claude/skills/autobuilder/scripts/"
  ],
  "non_goals": [
    "Spawning Claude subagents from the binary itself (that is the orchestrator's job)",
    "Web-service, WASM, or embedded scaffolding (v1 supports cli + lib only)",
    "Auto-applying self-evolution proposals (always gated for user review)",
    "Re-running the underlying audit/test commands inside `gate` (gate only attests; producers own the work)"
  ],
  "hard_constraints": {
    "rust_edition": "2024",
    "target_kind": "cli",
    "deny_unsafe": true,
    "max_deps": null,
    "msrv": "1.85",
    "additional": {
      "no_unwrap_in_src": true,
      "no_expect_in_src": true,
      "all_receipts_digest_bound": true
    }
  },
  "five_whys_trace": [
    {
      "why": 1,
      "q": "Why a Rust companion binary instead of a pure-skill scaffold?",
      "a": "Shell is too easy to subvert (set -e off, grep eaten, jq missing). The load-bearing parts \u2014 metric extraction, digest binding, schema validation \u2014 need a typed implementation under tests."
    },
    {
      "why": 2,
      "q": "Why a 7-receipt gate instead of `cargo test && cargo clippy`?",
      "a": "Each receipt covers a class of failure that the others miss: clippy doesn't see CI status, tests don't see proof-lane routing, and none of them produce an independent-review record. The gate is what makes 'ready' falsifiable."
    },
    {
      "why": 3,
      "q": "Why bootstrap autobuilder against itself before running it on other projects?",
      "a": "If the gate model can't certify the binary that produces gate receipts, it can't be trusted on any other project. The meta-bootstrap is the cheapest adversarial test of the whole pipeline."
    },
    {
      "why": 4,
      "q": "Why deny unwrap/expect everywhere in src/ when most of this is glue code?",
      "a": "Glue code that panics on a missing receipt or malformed JSON destroys the audit trail at exactly the moments the gate matters most. Erroring back to the caller keeps the diagnostic surface alive."
    },
    {
      "why": 5,
      "q": "Why digest-bind every receipt instead of trusting file timestamps?",
      "a": "Timestamps lie under git checkout. A self-binding sha256 lets the gate detect tampering and lets reviewers re-verify by recomputing on the receipt's own contents."
    }
  ],
  "created_at": "2026-05-22T08:10:00Z",
  "ambiguities_resolved": [
    {
      "question": "Where does the autobuilder repo's 'project' live for the gate \u2014 repo root or autobuilder/ workspace?",
      "resolution": "Repo root. agent/, scripts/, target/autobuilder/ all sit at /home/jsy/projects/autobuilder/. Proof-lanes globs use autobuilder/src/** prefix to match `git diff` output."
    }
  ]
}
