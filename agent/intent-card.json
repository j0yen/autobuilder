{
  "schema": "autobuilder.intent_card.v1",
  "prd_source": "/home/jsy/Documents/PRDs/build-queue/PRD-autobuilder-rollback-mechanical-commits.md",
  "intent_slug": "autobuilder-rollback-mechanical-commits",
  "root_motivation": "Six mcphost PRDs (`mcphost-tool-test`, `mcphost-metered-overage`, `mcphost-python-kind-runtime`, `mcphost-ci-sandbox-coverage`, `mcphost-healthz-minimal`, `mcphost-rest-bridge`, plus the meta-PRD `build-extend-gate-receipts`) have been re-hitting the identical `rollback-plan` gate block for hours across dozens of ticks today (2026-09-07). This tick fixed the sibling `vti-plan` blocker (a one-line unrouted-path fix, `d3668a0`) and confirmed `reviewer-agent`'s own block is just echoing `rollback-plan` \u2014 so `rollback-plan` is now the *only* root blocker left on mcphost, and it is a self-reinforcing trap, not real risk: `rollback-plan`'s base tag is pinned at `v0.13.3` (the newest tag reachable from HEAD) because nothing has passed the gate since, so every intent-card-refresh, `Cargo.lock` version-field sync, and \"parallel integrate\" version-bump commit from every sibling PRD keeps piling into the checked range (52-80 commits and counting), each one non-revert-clean only because a *late...",
  "user_persona": "The autobuilder skill orchestrator (a Claude session) and the human invoking `/autobuilder` from their own project. Both shell out to this binary for the receipt + gate steps that must not live in bash.",
  "unfakeable_metric": {
    "name": "stage4_receipt_producers_callable",
    "lower_is_better": false,
    "harness_command": "scripts/run-metrics.sh",
    "target": 5
  },
  "acceptance_criteria": [
    {
      "id": "AC1",
      "level": "MUST",
      "description": "Given a synthetic git repo (built inside the test, not depending on the live mcphost checkout) with a commit whose subject is `agent: refresh intent card for foo` and whose only changed file is `agent/intent-card.json`, When `rollback-plan` runs, Then that commit is classified `mechanical` regardless of whether its revert dry-run succeeds.",
      "test": "tests/acceptance_ac1.rs"
    },
    {
      "id": "AC2",
      "level": "MUST",
      "description": "Given the same fixture repo but the commit's changed-file set also includes `src/lib.rs`, When `rollback-plan` runs, Then that commit is classified `substantive` (the mechanical subject-line match alone must never launder an unexpected file change).",
      "test": "tests/acceptance_ac2.rs"
    },
    {
      "id": "AC3",
      "level": "MUST",
      "description": "Given a fixture repo where the ONLY non-revert-clean commits are mechanical (matching one of the three patterns, file-set-bounded), When `rollback-plan` runs, Then `verdict=pass` and `blocking_count=0`, even though `mechanical_count > 0` and some of those mechanical commits show `revertable=false`.",
      "test": "tests/acceptance_ac3.rs"
    },
    {
      "id": "AC4",
      "level": "MUST",
      "description": "Given a fixture repo with one substantive non-revert-clean commit (an ordinary `src/` change unrelated to any of the three patterns) plus several non-revert-clean mechanical commits, When `rollback-plan` runs, Then `verdict=block` and `blocking_count=1` (the mechanical ones are excluded, the substantive one still blocks) \u2014 proves the fix narrows scope without disabling the check.",
      "test": "tests/acceptance_ac4.rs"
    },
    {
      "id": "AC5",
      "level": "MUST",
      "description": "Given the fix landed and version bumped, When `extend-gate.sh /home/jsy/wintermute/mcphost --head <landed sha of this PRD's own commit, applied on top of current mcphost HEAD>` runs (or the equivalent direct `rollback-plan` invocation against mcphost's real history at that HEAD), Then `rollback-plan`'s verdict is `pass` (mcphost's non-revert-clean commits are dominated by the three mechanical shapes per the Problem section's direct verification \u2014 confirm no unexpected substantive blocker rema...",
      "test": "tests/acceptance_ac5.rs"
    },
    {
      "id": "AC6",
      "level": "MUST",
      "description": "Given all of the above, When `cargo test --workspace` and `cargo clippy --workspace --all-targets -- -D warnings` run on the diff, Then both are clean.",
      "test": "tests/acceptance_ac6.rs"
    }
  ],
  "scope": [
    "Stage 1 intake (intent-card validation)",
    "Stage 2 scaffold (templated project materialization)",
    "Stage 3 iterate-and-prove loop (advance/revert)",
    "Stage 4 risk gate (7 receipts + release receipt)",
    "Stage 5 postmortem + gated self-evolution",
    "Companion shell scripts in ~/.claude/skills/autobuilder/scripts/"
  ],
  "non_goals": [
    "Spawning Claude subagents from the binary itself (that is the orchestrator's job)",
    "Web-service, WASM, or embedded scaffolding (v1 supports cli + lib only)",
    "Auto-applying self-evolution proposals (always gated for user review)",
    "Re-running the underlying audit/test commands inside `gate` (gate only attests; producers own the work)"
  ],
  "hard_constraints": {
    "rust_edition": "2024",
    "target_kind": "cli",
    "deny_unsafe": true,
    "max_deps": null,
    "msrv": "1.85",
    "additional": {
      "no_unwrap_in_src": true,
      "no_expect_in_src": true,
      "all_receipts_digest_bound": true
    }
  },
  "five_whys_trace": [
    {
      "why": 1,
      "q": "Why a Rust companion binary instead of a pure-skill scaffold?",
      "a": "Shell is too easy to subvert (set -e off, grep eaten, jq missing). The load-bearing parts \u2014 metric extraction, digest binding, schema validation \u2014 need a typed implementation under tests."
    },
    {
      "why": 2,
      "q": "Why a 7-receipt gate instead of `cargo test && cargo clippy`?",
      "a": "Each receipt covers a class of failure that the others miss: clippy doesn't see CI status, tests don't see proof-lane routing, and none of them produce an independent-review record. The gate is what makes 'ready' falsifiable."
    },
    {
      "why": 3,
      "q": "Why bootstrap autobuilder against itself before running it on other projects?",
      "a": "If the gate model can't certify the binary that produces gate receipts, it can't be trusted on any other project. The meta-bootstrap is the cheapest adversarial test of the whole pipeline."
    },
    {
      "why": 4,
      "q": "Why deny unwrap/expect everywhere in src/ when most of this is glue code?",
      "a": "Glue code that panics on a missing receipt or malformed JSON destroys the audit trail at exactly the moments the gate matters most. Erroring back to the caller keeps the diagnostic surface alive."
    },
    {
      "why": 5,
      "q": "Why digest-bind every receipt instead of trusting file timestamps?",
      "a": "Timestamps lie under git checkout. A self-binding sha256 lets the gate detect tampering and lets reviewers re-verify by recomputing on the receipt's own contents."
    }
  ],
  "created_at": "2026-05-22T08:10:00Z",
  "ambiguities_resolved": [
    {
      "question": "Where does the autobuilder repo's 'project' live for the gate \u2014 repo root or autobuilder/ workspace?",
      "resolution": "Repo root. agent/, scripts/, target/autobuilder/ all sit at /home/jsy/projects/autobuilder/. Proof-lanes globs use autobuilder/src/** prefix to match `git diff` output."
    }
  ]
}
